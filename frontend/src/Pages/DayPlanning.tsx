import { Flex } from "@chakra-ui/react";
import Navbar from "../Components/Navbar";

const DayPlanning = () => {
  return (
    <Flex>
      <Navbar />
    </Flex>
  );
};

export default DayPlanning;
